// Self-hosted via @fontsource (files ship inside the npm package), so
// rendering never depends on a network fetch to Google's font CDN.
import '@fontsource/inter/400.css';
import '@fontsource/inter/600.css';
import '@fontsource/inter/800.css';

export const interFont = 'Inter';
